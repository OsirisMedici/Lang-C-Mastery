#include <stdio.h>

// it's a comment so I passed right??

int main(int argc, char const *argv[])
{
    int cube;
    printf("Enter a side of Cube: ");
    scanf ("%d", &cube);
    printf("answer of cube is : %d", (cube*cube*cube));
    return 0;
}
